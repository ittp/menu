let active = { jsonrpc: "2.0", method: "Player.GetActivePlayers", id: 1 };

class Player {
  constructor(props) {
      this.config = {
         jsonrpc: "2.0" 
      }
  }
  GetActivePlayers() {}
}

let GetActivePlayers = () => {};
